
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile information, excluding authentication credentials which are managed by an external system like Firebase Auth.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity. This typically maps to the Firebase Auth User ID (uid)."
        },
        "displayName": {
          "type": "string",
          "description": "The user's preferred display name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL to the user's profile picture or avatar.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "displayName",
        "email",
        "createdAt",
        "updatedAt"
      ]
    },
    "UserProgress": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProgress",
      "type": "object",
      "description": "Tracks a specific user's progress through a ReadingUnit within a ReadingPath, including their current stage in the 'Guided Ascent'.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProgress entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile making the progress."
        },
        "readingUnitId": {
          "type": "string",
          "description": "Reference to the ReadingUnit for which progress is being tracked."
        },
        "currentStage": {
          "type": "string",
          "description": "The current stage of the 'Guided Ascent' (e.g., 'Read', 'Understand', 'Master')."
        },
        "startedAt": {
          "type": "string",
          "description": "Timestamp when the user first started this reading unit.",
          "format": "date-time"
        },
        "lastAccessedAt": {
          "type": "string",
          "description": "Timestamp when this reading unit was last accessed.",
          "format": "date-time"
        },
        "completedAt": {
          "type": "string",
          "description": "Optional timestamp when the 'Master' stage was achieved.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "readingUnitId",
        "currentStage",
        "startedAt",
        "lastAccessedAt"
      ]
    },
    "Annotation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Annotation",
      "type": "object",
      "description": "A user-created note or highlight on a specific scripture passage.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the annotation."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the authoring UserProfile."
        },
        "passageRef": {
          "type": "string",
          "description": "The scripture reference (e.g., 'John 3:16') this annotation belongs to."
        },
        "content": {
          "type": "string",
          "description": "The text content of the annotation."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the annotation was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "passageRef",
        "content",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Private user profile data."
    },
    "/users/{userId}/user_progress/{progressId}": {
      "schema": "UserProgress",
      "description": "Private study progress tracking."
    },
    "/users/{userId}/annotations/{annotationId}": {
      "schema": "Annotation",
      "description": "Private study notes and verse highlights."
    }
  }
}
